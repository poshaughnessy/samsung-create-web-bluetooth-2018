{
  "name": "samsung-create-web-bluetooth-2018",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "start": "npm run watch & npm run serve",
    "serve": "node server",
    "serve-bluetooth": "node server --bluetooth",
    "build": "cleaver src/index.md && echo 'Finished cleaver build'",
    "build:debug": "cleaver --debug src/index.md && echo 'Finished cleaver build'",
    "watch": "watch 'npm run build' src"
  },
  "devDependencies": {
    "bleno": "^0.5.0",
    "cleaver": "^0.8.7",
    "express": "^4.16.3",
    "watch": "^1.0.2"
  },
  "dependencies": {
    "faye": "^1.2.4"
  }
}
